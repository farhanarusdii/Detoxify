<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/detox_primary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⏱"
            android:textSize="56sp" />

        <TextView
            android:id="@+id/tv_lock_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="@string/phone_lock_title"
            android:textColor="@color/detox_on_header"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_lock_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.2"
            android:text="@string/phone_lock_message"
            android:textColor="@color/detox_on_header_muted"
            android:textSize="15sp" />

        <com.google.android.material.card.MaterialCardView
            style="@style/Detoxify.Card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            app:cardBackgroundColor="@color/detox_surface">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <Button
                    android:id="@+id/btn_parent_unlock"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/phone_lock_parent_unlock"
                    style="@style/Widget.MaterialComponents.Button" />

                <Button
                    android:id="@+id/btn_request_access"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_8"
                    android:text="@string/phone_lock_request_access" />

                <!--
                    Offline activities shortcut — opens BedtimeIdeasActivity in child mode.
                    Launched with EXTRA_FROM_LOCK_SCREEN=true so BedtimeIdeasActivity skips
                    the redirectIfGated() guard and stays open while the phone is locked.
                    Uses a text button (lowest visual weight) so it reads as a tertiary action
                    beneath the two primary/outlined buttons above it.
                -->
                <Button
                    android:id="@+id/btn_what_can_i_do"
                    style="@style/Widget.MaterialComponents.Button.TextButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_4"
                    android:text="@string/phone_lock_what_can_i_do"
                    android:drawableStart="@drawable/ic_lightbulb"
                    android:drawablePadding="6dp" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:gravity="center"
                    android:text="@string/phone_lock_footer"
                    android:textColor="@color/detox_text_secondary"
                    android:textSize="12sp" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>
</FrameLayout>